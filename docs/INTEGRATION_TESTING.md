# Integration testing

Moved. See [TESTING.md](TESTING.md#3-integration--gui-tests).

Entry point: `dart run_integration_tests.dart`. It starts and stops the browser driver
itself — the manual `chromedriver` step this file used to describe is no longer needed.
